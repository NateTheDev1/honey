{
  "name": "create-honey-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "bin": {
    "create-honey-app": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^20.11.10",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "rm -rf dist && tsc && chmod +x dist/index.js && mkdir ./dist/templates && cp -r ./templates/* ./dist/templates"
  }
}